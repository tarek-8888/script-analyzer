# script-analyzer
this repo for NETWORK SECURITY its basic but upgradable ... this project for CI/ , can try it locally on your pc linux os :
no python lib need to install.
use "./ids-script.sh" or "bash ids-script.sh" to run bash script shell.
