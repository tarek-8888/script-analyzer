name: Simple CI

on:
  push:
    branches: ["main"]

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: run ids-script
        run: |
          chmod +x ids-script.sh
          ./ids-script.sh
